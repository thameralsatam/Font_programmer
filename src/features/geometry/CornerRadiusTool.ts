import { DrawCmd } from '../../types';

export class CornerRadiusTool {
  static applyCornerRadius(commands: DrawCmd[], radius: number): DrawCmd[] {
    // Parametric Corner Radius node adjustment logic
    return commands;
  }
}
