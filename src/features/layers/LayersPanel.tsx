import React from 'react';

export function LayersPanel() {
  return (
    <div className="p-6 bg-zinc-50 border border-zinc-200 rounded-3xl text-center" dir="rtl">
      <h3 className="text-sm font-bold text-zinc-800">إدارة طبقات الرسم (Layers)</h3>
      <p className="text-xs text-zinc-500 mt-2">سيتم تفعيل لوحة الطبقات لفرز طبقات التصميم، والمسارات الاحتياطية، والأدلة التوضيحية لاحقاً.</p>
    </div>
  );
}
